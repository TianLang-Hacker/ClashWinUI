<?xml version="1.0" encoding="utf-8"?>
<Page
    x:Class="ClashWinUI.Views.Pages.ProfilesPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:models="using:ClashWinUI.Models"
    mc:Ignorable="d">

    <Grid Background="Transparent">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <Grid Margin="24,24,24,12">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <StackPanel
                Grid.Column="1"
                Orientation="Horizontal"
                Spacing="8"
                VerticalAlignment="Center">
                <Button
                    Command="{Binding RefreshCommand}"
                    Content="{Binding [ProfilesRefreshButton], Source={StaticResource L}}" />
                <Button
                    Click="ImportLocalFileButton_Click"
                    Content="{Binding [ProfilesImportLocalButton], Source={StaticResource L}}" />
                <Button
                    Command="{Binding DeleteSelectedCommand}"
                    Content="{Binding [ProfilesDeleteButton], Source={StaticResource L}}" />
            </StackPanel>
        </Grid>

        <StackPanel
            Grid.Row="1"
            Margin="24,0,24,12"
            Spacing="8">
            <TextBlock Text="{Binding [ProfilesSubscriptionUrlLabel], Source={StaticResource L}}" />
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>

                <TextBox
                    Grid.Column="0"
                    Text="{Binding SubscriptionUrl, Mode=TwoWay}"
                    PlaceholderText="{Binding [ProfilesSubscriptionUrlPlaceholder], Source={StaticResource L}}" />
                <Button
                    Grid.Column="1"
                    Margin="8,0,0,0"
                    Command="{Binding FetchSubscriptionCommand}"
                    Content="{Binding [ProfilesFetchButton], Source={StaticResource L}}" />
            </Grid>
        </StackPanel>

        <StackPanel
            Grid.Row="2"
            Margin="24,0,24,12"
            Spacing="6">
            <TextBlock Text="{Binding ActiveProfileText, Mode=OneWay}" />
            <TextBlock
                Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                Text="{Binding StatusMessage, Mode=OneWay}"
                TextWrapping="WrapWholeWords" />
        </StackPanel>

        <Border
            Grid.Row="3"
            Margin="24,0,24,24"
            Padding="4"
            CornerRadius="8"
            Background="{ThemeResource CardBackgroundFillColorDefaultBrush}">
            <ListView
                ItemsSource="{Binding Profiles}"
                SelectedItem="{Binding SelectedProfile, Mode=TwoWay}"
                BorderThickness="0"
                HorizontalContentAlignment="Stretch"
                Background="Transparent">
                <ListView.ItemTemplate>
                    <DataTemplate x:DataType="models:ProfileItem">
                        <Grid Margin="4,4,4,2">
                            <Grid.ColumnDefinitions>
                                <ColumnDefinition Width="*" />
                                <ColumnDefinition Width="Auto" />
                            </Grid.ColumnDefinitions>

                            <StackPanel>
                                <TextBlock
                                    FontWeight="SemiBold"
                                    Text="{Binding DisplayName}" />
                                <TextBlock
                                    Foreground="{ThemeResource TextFillColorSecondaryBrush}"
                                    Text="{Binding Source}"
                                    TextWrapping="WrapWholeWords" />
                            </StackPanel>

                            <StackPanel
                                Grid.Column="1"
                                HorizontalAlignment="Right"
                                Margin="8,0,0,0">
                                <TextBlock Text="{Binding SourceType}" />
                                <TextBlock Text="{Binding NodeCount}" />
                            </StackPanel>
                        </Grid>
                    </DataTemplate>
                </ListView.ItemTemplate>
            </ListView>
        </Border>
    </Grid>
</Page>
